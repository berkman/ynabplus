<a href="/">Home</a>
<p><%= @budget.name %></p>
<h1>Accounts</h1>
<% @accounts.sort { |a,b| a.name <=> b.name }.each do |account| %>
  <p><%= account.name %>, <%= account.id %>, <%= account.balance %></p>
<% end %>
